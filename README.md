# Mock ESIA (ЕСИА) authorization server

## Development

**Build and run:**

```shell
./gradlew clean bootRun
```

**Format:**

```shell
./gradlew ktfmtFormat     
```


## Deployment

```shell
docker compose up -d --wait --build
```